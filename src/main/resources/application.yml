spring:
  datasource:
    url: jdbc:postgresql://database:5432/androidDB
    username: admin
    password: admin
    driverClassName: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true